eureka:
  client:
    service-url:
      defaultZone: http://localhost:6761/eureka/
#      defaultZone: http://web-registry-service:6761/eureka/
    healthcheck:
      enabled: 'true'
    enabled: 'true'
    fetch-registry: 'true'
    register-with-eureka: 'true'
    lease:
      duration: '5'
  instance:
    prefer-ip-address: 'true'
    lease-renewal-interval-in-seconds: '1'
    lease-expiration-duration-in-seconds: '2'
spring:

  jpa:
    properties:
      hibernate:
        dialect:
          storage_engine: innoDb
          nodeValue: org.hibernate.dialect.MySQL5InnoDBDialect
        current_session_context_class: org.springframework.orm.hibernate5.SpringSessionContext
        hbm2ddl:
          auto: update
      max_allowed_packet: '2000'
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: 'true'
  datasource:
#    password:
#    url: jdbc:mysql://localhost:3306/life_forms?createDatabaseIfNotExist=true&useSSL=false&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&failOverReadOnly=false&allowMultiQueries=true&serverTimezone=Africa/Harare
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
    password : Foxcode@7
    url : jdbc:mysql://10.1.4.4:3306/life_forms?createDatabaseIfNotExist=true&useSSL=false&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&failOverReadOnly=false&allowMultiQueries=true&serverTimezone=Africa/Harare
    driver-class-name: com.mysql.cj.jdbc.Driver
    username : dev_user
  main:
    allow-bean-definition-overriding: 'true'

  feign:
    user:
      client: http://user-service:6101/
jwt:
  get:
    token:
      uri: /authentication
  http:
    request:
      header: Authorization
  refresh:
    token:
      expiration:
        in:
          minutes: '1576800'
      uri: /refresh
  signing:
    key:
      secret: '#$%&t2019#$'
  token:
    expiration:
      in:
        seconds: '10800'

security:
  oauth2:
    client:
      client-secret: RobTheRich
      client-id: life-form
app:
  jwtExpirationInMs: '1.8e+7'
  jwtSecret: JWTSuperSecretKey
com:
  sun:
    security:
      enableAIAcaIssuers: 'true'
springdoc:
  swagger-ui:
    path: /swagger-ui.html